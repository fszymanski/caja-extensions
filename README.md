## Installation

```sh
$ sudo {apt,dnf} install python3-caja
$ git clone https://github.com/fszymanski/caja-extensions.git
$ mkdir -p ~/.local/share/caja-python/extensions
$ cp path/to/extension ~/.local/share/caja-python/extensions/
```
