## Installation

```sh
$ sudo {apt,dnf} install python3-caja
$ git clone https://github.com/fszymanski/caja-plugins.git
$ mkdir -p ~/.local/share/caja-python/extensions
$ cp path/to/plugin ~/.local/share/caja-python/extensions/
```
